apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /vote
            backend:
              service:
                name: vote
                port:
                  number: 80
          - pathType: Prefix
            path: /result
            backend:
              service:
                name: result
                port:
                  number: 80
